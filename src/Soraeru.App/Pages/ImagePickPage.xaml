<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Soraeru.Pages.ImagePickPage"
             Title="圖片取字"
             Shell.NavBarIsVisible="True"
             BackgroundColor="{StaticResource SurfaceBright}">

    <Grid RowDefinitions="*,Auto">
        <ScrollView Grid.Row="0">
            <VerticalStackLayout Padding="16,16" Spacing="12" MaximumWidthRequest="480" HorizontalOptions="Center">

                <Border Stroke="{StaticResource OutlineVariant}"
                        StrokeThickness="1"
                        StrokeShape="RoundRectangle 12"
                        BackgroundColor="{StaticResource SurfaceBright}"
                        MinimumHeightRequest="280">
                    <Grid>
                        <VerticalStackLayout x:Name="PreviewPlaceholder"
                                             VerticalOptions="Center"
                                             HorizontalOptions="Center"
                                             Spacing="10"
                                             Padding="24">
                            <Border StrokeThickness="0"
                                    BackgroundColor="{StaticResource PrimaryContainer}"
                                    HeightRequest="64"
                                    WidthRequest="64"
                                    StrokeShape="RoundRectangle 32"
                                    HorizontalOptions="Center">
                                <Image Source="ic_photo_camera.png"
                                       HeightRequest="32"
                                       WidthRequest="32"
                                       Aspect="AspectFit"
                                       HorizontalOptions="Center"
                                       VerticalOptions="Center" />
                            </Border>
                            <Label Text="選擇或拍攝圖片"
                                   Style="{StaticResource SectionTitle}"
                                   HorizontalTextAlignment="Center" />
                            <Label Text="請確保文字清晰可見，以獲得最佳辨識效果。"
                                   Style="{StaticResource CaptionLabel}"
                                   HorizontalTextAlignment="Center"
                                   LineBreakMode="WordWrap" />
                        </VerticalStackLayout>
                        <Image x:Name="PreviewImage"
                               IsVisible="False"
                               Aspect="AspectFit" />
                    </Grid>
                </Border>

                <Grid ColumnDefinitions="*,*" ColumnSpacing="12" Margin="0,4,0,0">
                    <Border Grid.Column="0"
                            Stroke="{StaticResource OutlineVariant}"
                            StrokeThickness="1"
                            StrokeShape="RoundRectangle 12"
                            BackgroundColor="{StaticResource SurfaceContainerLow}"
                            Padding="12,20">
                        <Border.GestureRecognizers>
                            <TapGestureRecognizer Tapped="OnCameraClicked" />
                        </Border.GestureRecognizers>
                        <VerticalStackLayout Spacing="8" HorizontalOptions="Center">
                            <Image Source="ic_photo_camera.png"
                                   HeightRequest="28"
                                   WidthRequest="28"
                                   Aspect="AspectFit"
                                   HorizontalOptions="Center" />
                            <Label Text="拍照"
                                   TextColor="{StaticResource OnSurface}"
                                   FontSize="14"
                                   FontAttributes="Bold"
                                   HorizontalTextAlignment="Center" />
                        </VerticalStackLayout>
                    </Border>
                    <Border Grid.Column="1"
                            Stroke="{StaticResource OutlineVariant}"
                            StrokeThickness="1"
                            StrokeShape="RoundRectangle 12"
                            BackgroundColor="{StaticResource SurfaceContainerLow}"
                            Padding="12,20">
                        <Border.GestureRecognizers>
                            <TapGestureRecognizer Tapped="OnGalleryClicked" />
                        </Border.GestureRecognizers>
                        <VerticalStackLayout Spacing="8" HorizontalOptions="Center">
                            <Image Source="ic_style.png"
                                   HeightRequest="28"
                                   WidthRequest="28"
                                   Aspect="AspectFit"
                                   HorizontalOptions="Center" />
                            <Label Text="相簿"
                                   TextColor="{StaticResource OnSurface}"
                                   FontSize="14"
                                   FontAttributes="Bold"
                                   HorizontalTextAlignment="Center" />
                        </VerticalStackLayout>
                    </Border>
                </Grid>

                <VerticalStackLayout Spacing="6" Margin="0,4,0,0">
                    <Label Text="語系別（選填）"
                           Style="{StaticResource FieldLabel}" />
                    <Border Stroke="{StaticResource OutlineVariant}"
                            StrokeThickness="1"
                            StrokeShape="RoundRectangle 8"
                            BackgroundColor="{StaticResource Surface}"
                            Padding="12,4">
                        <Picker x:Name="ScriptFamilyPicker"
                                Title="語系別"
                                SelectedIndex="0"
                                TextColor="{StaticResource OnSurface}"
                                TitleColor="{StaticResource Outline}"
                                FontSize="16">
                            <Picker.Items>
                                <x:String>自動</x:String>
                                <x:String>拉丁字母（英／法／德／西…）</x:String>
                                <x:String>西里爾（俄／烏…）</x:String>
                                <x:String>漢字體系（中／日／韓）</x:String>
                                <x:String>阿拉伯字母（阿／波…）</x:String>
                                <x:String>天城文（印地／尼泊爾…）</x:String>
                                <x:String>東南亞（泰／緬／寮／柬）</x:String>
                                <x:String>其他</x:String>
                            </Picker.Items>
                        </Picker>
                    </Border>
                    <Label Text="預設自動。不確定時選自動即可；也可依文字長相選語系別，讓本機辨識走對路線（例如阿拉伯字母→ara、西里爾→rus）。原圖不上傳；缺少語言包時會下載至本機快取。"
                           Style="{StaticResource CaptionLabel}"
                           LineBreakMode="WordWrap" />
                </VerticalStackLayout>

                <Border StrokeThickness="0"
                        BackgroundColor="{StaticResource SurfaceContainerLowest}"
                        StrokeShape="RoundRectangle 0,8,0,8"
                        Padding="0"
                        Margin="0,8,0,0">
                    <Grid ColumnDefinitions="4,*">
                        <BoxView Grid.Column="0"
                                 Color="{StaticResource StatusInfo}" />
                        <Label Grid.Column="1"
                               Text="圖片僅在手機端辨識，不會上傳至伺服器。保障您的資料隱私與安全。"
                               TextColor="{StaticResource OnSurfaceVariant}"
                               FontSize="12"
                               Padding="12,14"
                               LineBreakMode="WordWrap" />
                    </Grid>
                </Border>

                <ActivityIndicator x:Name="BusyIndicator"
                                   IsRunning="False"
                                   IsVisible="False"
                                   Color="{StaticResource Primary}" />
                <Label x:Name="StatusLabel"
                       Style="{StaticResource CaptionLabel}"
                       HorizontalTextAlignment="Center" />
            </VerticalStackLayout>
        </ScrollView>

        <Grid Grid.Row="1"
              Style="{StaticResource StickyBar}"
              BackgroundColor="{StaticResource SurfaceBright}">
            <Button x:Name="StartOcrButton"
                    Text="✨ 開始辨識"
                    Style="{StaticResource PrimaryButton}"
                    CornerRadius="28"
                    Clicked="OnOcrClicked" />
        </Grid>
    </Grid>
</ContentPage>
